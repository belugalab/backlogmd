# Agent Rules

- Always delegate to specialized agents: `@backlog`
- Use `@backlog` for any backlog-related tasks (create, update, complete features/tasks)
- In plan mode: analyze and delegate only
- In build mode: execute changes
